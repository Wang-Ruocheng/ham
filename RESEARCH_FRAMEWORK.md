# 结构化哈密顿神经网络 (Structured HNN) 研究框架

## 一、研究动机

传统 HNN 将整个系统哈密顿量用单一黑箱网络学习，面临两个核心问题：

1. **参数效率低**：全连接网络参数量大，难以从有限数据中学习
2. **无法处理耗散**：辛结构天然保持能量守恒，无法建模耗散系统

**核心策略**：将已知物理结构硬编码，仅让网络学习未知部分。

```
H_total = H_pend(q1,p1) + H_ho(q2,p2) + H_coup(q1,p1,q2,p2)
          └── MLP 学习 ──┘  └── 解析硬编码 ──┘  └── MLP 学习 ──┘
```

---

## 二、研究路线图

```
基线: 固定 ω + 保守数据
└── hnn_structured.py
    │
    ├── Step 1: 可学习 ω → 参数效率
    │   └── hnn_structured_learnable_omega.py
    │
    ├── Step 2: 保守模型 + 耗散数据 → 耦合能否隐式学习耗散？
    │   └── hnn_structured_damped.py
    │
    └── Step 3: 复数 ω → 有限维度单向耗散
        └── hnn_structured_complex_omega.py ★
```

---

## 三、各阶段详解

### Step 0: 基线 — 结构化 HNN (保守系统)

**文件**: `hnn_structured.py`

**物理系统** (保守):
```
dp1/dt = -sin(q1) - ε·q2    ← 单摆
dq1/dt = p1
dq2/dt = p2
dp2/dt = -ω²·q2 - ε·q1      ← 谐振子
```

**模型**:
```
H = H_pend(MLP) + ½p2² + ½ω²q2² + H_coup(MLP)
     ↑ 黑箱     └── 已知，硬编码 ──┘  ↑ 黑箱

d/dt = J·∇H  (完全辛结构)
```

**结果**: 成功学习 Pendulum 哈密顿量和耦合项，轨迹预测与真实物理一致。

**输出**: `structured_loss.png`, `structured_evaluation.png`, `hamiltonian_surface.png`, `trajectory_evaluation.png`

---

### Step 1: 可学习 ω vs 固定 ω

**文件**: `hnn_structured_learnable_omega.py`

**核心问题**: 将 ω 从硬编码改为可学习参数（仅多 1 个参数），能否提升性能？

**对比**:
| 方案 | ω | 额外参数 |
|------|---|---------|
| 固定 ω | 2.0 (硬编码) | 0 |
| 可学习 ω | 初始 2.0, 参与训练 | 1 |

**结论**: 在保守系统上，可学习 ω 与固定 ω 性能相当 — 因为数据本身确定后，最优 ω 就是真实值，无需额外学习。

---

### Step 2: 保守 HNN 处理耗散数据

**文件**: `hnn_structured_damped.py`

**核心问题**: 数据含有耗散 (γ·p1)，但 HNN 是保守的。H_coup MLP 能否隐含学习耗散效应？

**物理系统** (非保守):
```
dp1/dt = -sin(q1) - γ·p1 - ε·q2   ← 阻尼项 γ·p1
```

**模型**: 完全保守 (d/dt = J·∇H)，无任何耗散项。

**结论**: 保守 HNN 无法处理耗散。MSE 被 γ·p1 残差拖死，需引入耗散通道。

**输出**: `damped_loss.png`, `damped_evaluation.png`

---

### Step 3: 复数 ω — 有限维度单向耗散 ★

**文件**: `hnn_structured_complex_omega.py`

#### 3.1 核心思想 (Caldeira–Leggett 图像)

将 HO 频率设为复数 ω = ω_R + iω_I：

```
实数 ω → HO 与 Pendulum 间只有周期性 Rabi 能量交换
复数 ω → Im(ω) 引入 HO 耗散通道，能量单向流动：
          Pendulum → HO → 耗散 (不可逆)
```

#### 3.2 物理系统 (数据生成)

```
dp1/dt = -sin(q1) - ε·q2           ← 保守 (无显式阻尼)
dq1/dt = p1
dq2/dt = p2
dp2/dt = -ω²·q2 - ε·q1 - γ·p2      ← 阻尼在 HO 上

dE/dt = -γ·p2² ≤ 0
```

**关键**: 阻尼在 HO 上，与 Complex ω 模型的耗散项对齐。

#### 3.3 三种模型对比

| 方案 | Pendulum | HO 哈密顿量 | 运动方程 | 耗散 |
|------|----------|------------|---------|------|
| **A: Fixed ω** | HNN | ½p2² + ½ω²q2² (ω=2.0) | d/dt = J·∇H | ✗ |
| **B: Real ω** | HNN | ½p2² + ½ω²q2² (ω 可学习) | d/dt = J·∇H | ✗ |
| **C: Complex ω** | HNN | ½p2² + ½(ω_R²+ω_I²)q2² | dp2/dt = -∂H/∂q2 − 2ω_I·p2 | ✓ |

#### 3.4 数学推导

复数 ω 的物理含义：考虑 HO 的复坐标 `z = q2 + i·p2/ω`，运动方程：
```
dz/dt = -iωz = -i(ω_R + iω_I)z = -iω_R z + ω_I z
```

实部 ω_R 给出振荡，虚部 ω_I 给出指数衰减。写回实变量：
```
dq2/dt = p2
dp2/dt = -ω_R² q2 - 2ω_I·p2
```

或等价地，保持哈密顿量 `H_ho = ½p2² + ½(ω_R²+ω_I²)q2²` 为实数，在运动方程中显式添加阻尼：
```
dp2/dt = -∂H_ho/∂q2 - 2ω_I·p2
```

#### 3.5 实验结果

```
方案                   | 参数数 | 测试 MSE    | ω
────────────────────────────────────────────────────────────
固定 ω (实数)          | 114992 | 1.0074e-04 | 2.0 (固定)
可学习 ω (实数)        | 114993 | 1.0545e-04 | 1.8728
可学习 ω (复数)        | 114994 | 1.3268e-05 | 1.8747 + i·0.0501
────────────────────────────────────────────────────────────
>> 复数 ω 最优! MSE = 1.3268e-05
>> Im(ω) = 0.0501 → 学到的 HO 阻尼 = 0.1002 (真实 γ_HO = 0.1)
```

**关键发现**:
1. **MSE 低一个数量级**: 1.33e-5 vs 1.0e-4（Fixed/Real）
2. **阻尼完美恢复**: `2ω_I = 0.1002 ≈ γ_HO = 0.1`，误差仅 0.2%
3. **Fixed/Real ω 无法拟合耗散**: 保守 HO 的 dp2/dt 无法匹配 γ·p2 项

#### 3.6 输出文件

- `complex_omega_comparison.png` — 训练曲线 + MSE 柱状图 + |ω| 对比
- `complex_omega_trajectories.png` — 2×3 布局 (相空间 + 时间序列)

---

## 四、核心洞察

### 4.1 结构化 HNN 的优势

```
全连接 HNN:  H = MLP(q1,p1,q2,p2)  →  66,000+ 参数，需大量数据
结构化 HNN:  H = MLP(q1,p1) + ½p2² + ½ω²q2² + MLP(q1,p1,q2,p2)
               └── 已知结构硬编码，大幅减少黑箱参数
```

### 4.2 耗散建模的范式转变

```
传统思路:  HNN → 加耗散项到每个变量 → 破坏结构
新思路:    HNN → 耦合耗散谐振子 → 耗散集中在 HO 上
          Pendulum (保守) → HO (耗散) → 单向能量流
```

### 4.3 复数 ω 的物理意义

| 实数 ω | 复数 ω = ω_R + iω_I |
|--------|---------------------|
| 可逆 Rabi 振荡 | 单向耗散 |
| 能量在 Pendulum ↔ HO 间来回交换 | 能量从 Pendulum → HO → 不可逆耗散 |
| 时间反演对称 | 时间反演破缺 |

---

## 五、文件清单

| 文件 | 功能 |
|------|------|
| `hnn.py` | 基础 HNN 实现 |
| `hnn_structured.py` | Step 0: 结构化 HNN 基线 |
| `hnn_structured_learnable_omega.py` | Step 1: 可学习 ω 对比 |
| `hnn_structured_damped.py` | Step 2: 保守 HNN + 耗散数据 |
| `hnn_structured_complex_omega.py` | **Step 3: 复数 ω 耗散模型 ★** |
| `RESEARCH_FRAMEWORK.md` | 本文档 |

---

## 六、下一步方向

1. **多模耗散**：多个 HO 耦合，形成离散谱密度 J(ω)
2. **非马尔可夫动力学**：少模 HO 可产生记忆效应
3. **量子推广**：复数 ω → 非厄米哈密顿量的量子耗散
4. **高维系统**：将框架推广到更多自由度
